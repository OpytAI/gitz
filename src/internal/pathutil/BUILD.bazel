load("@rules_zig//zig:defs.bzl", "zig_library", "zig_test")

# Internal path handling and path security.
zig_library(
    name = "pathutil",
    main = "root.zig",
    srcs = [
        "dotgit.zig",
        "hfs.zig",
        "ntfs.zig",
        "tilde.zig",
        "tree.zig",
    ],
    import_name = "pathutil",
    visibility = ["//visibility:public"],
)

zig_test(
    name = "pathutil_test",
    size = "small",
    main = "root.zig",
    srcs = [
        "dotgit.zig",
        "hfs.zig",
        "ntfs.zig",
        "tilde.zig",
        "tree.zig",
    ],
)

# Sources for //check file/API inventory (parent globs do not cross package borders).
filegroup(
    name = "package_srcs",
    srcs = glob(["**/*"]),
    visibility = ["//visibility:public"],
)

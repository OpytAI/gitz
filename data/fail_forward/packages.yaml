# Frozen fixture for //check:*_fail_forward (not production inventory).
# Keep this stable so fail-forward does not depend on live src/.
pin: v5.19.2

packages:
  - go: fixture/alpha
    zig: src/fixture/alpha
    status: required
  - go: fixture/beta
    zig: src/fixture/beta
    status: required
  - go: fixture/gamma
    zig: src/fixture/gamma
    status: required
  - go: fixture/delta
    zig: src/fixture/delta
    status: required
  - go: fixture/excluded
    status: excluded
